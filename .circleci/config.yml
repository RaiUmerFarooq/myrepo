version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - run: python main.py
  test:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - run: python main.py
  deploy:
    docker:
      - image: cimg/python:3.11
    steps:
       run: echo "Deploying to production server"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build